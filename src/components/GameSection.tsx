import React from 'react';
import TableSection from './TableSection';

const GameSection: React.FC = () => {
  return (
    <TableSection themeColor={'#63C6FF'} />
  );
};

export default GameSection;
